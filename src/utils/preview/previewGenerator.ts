export const detectMainFile = (files: any[]) => {
  return null;
};

export const findPreviewHtmlFile = (files: any[]) => {
  return null;
};

export const generatePreviewHtmlShell = (content: string) => {
  return content;
};
