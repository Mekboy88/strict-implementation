export const streamCodeToEditor = (code: string, editor: any) => {
  // Placeholder for streaming code to editor
  console.log('Streaming code:', code);
};

export const cleanupStreamingInterval = (interval: any) => {
  if (interval) {
    clearInterval(interval);
  }
};
